#!/usr/bin/env bash

#read ~/Xmodmap if it exists
if [[ -f "${HOME}/.Xmodmap" ]]; then
	xmodmap "${HOME}/.Xmodmap"
fi

#if comfortable-swipe program exists

if [[ -f "${HOME}/comfortable-swipe" ]]; then
	comfortable-swipe start
fi
